## 🧭 从零基础学习这个项目的完整路径

你现在的项目已经是一个功能完整的 **数据流任务管理系统 MVP**，技术栈涉及：前端（HTML/CSS/JS + Canvas 画布）、后端（Java Spring Boot）、流计算引擎（Flink）、AI 侧车（Python FastAPI）。

下面我把学习路径分成 **5 个阶段**，每个阶段只学本阶段需要的最小知识量，学完就能看懂对应部分的代码。

------

### 📘 阶段一：先跑起来，建立整体感知（1-2天）

**目标**：不管代码，先让项目在本地跑通，看到效果。

1. **启动后端** — 打开终端执行：

   ```
   cd backend
   mvn spring-boot:run
   ```

   看到 `Started MvpBackendApplication` 说明成功

2. **打开前端** — 直接用浏览器打开 `frontend/public/index.html`

3. **玩一遍功能**：

   - 从左侧控件库拖一个「CSV 输入」到画布
   - 再拖一个「字段拼接」和一个「CSV 输出」
   - 连线：CSV输入 → 字段拼接 → CSV输出
   - 保存、导出 JSON、看控件管理页

4. **看 API 交互**：打开浏览器 F12 → Network 标签，看看前端调了哪些后端接口

> 🎯 **这阶段不写一行代码**，只建立"这个项目到底长什么样"的整体印象

------

### 📗 阶段二：前端入门 — 从看懂画布开始（3-5天）

这个项目的前端是一个 **单页应用**，所有代码都在一个文件里：`frontend/public/js/app.js`

**先学前置知识（不要多学，够用就行）：**

| 要学的          | 学多少                              | 为什么                   |
| --------------- | ----------------------------------- | ------------------------ |
| HTML 基础       | 标签、属性、DOM 结构                | 看懂 `index.html`        |
| CSS 基础        | 选择器、flexbox                     | 看懂 `style.css`         |
| JavaScript 基础 | 变量、函数、数组、对象、async/await | 看懂 `app.js`            |
| Canvas 2D API   | 画矩形、画圆、画线、贝塞尔曲线      | 画布就是用 Canvas 手绘的 |

**学习资料（按顺序）：**

- [MDN JavaScript 教程](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide) — 只看"语法与类型、流程控制、函数、对象、Promise"这五节
- [Canvas 教程](https://developer.mozilla.org/zh-CN/docs/Web/API/Canvas_API/Tutorial) — 只看"绘制图形、添加样式和颜色、绘制文本"三节

**然后对照项目代码阅读：**

```
app.js 读代码顺序（从浅到深）:
1. 最上面的 API 函数（→ 学 fetch 怎么调用后端）
2. ht() 命中检测函数（→ 学坐标计算，最简单的逻辑）
3. _d() 绘制函数（→ 学 Canvas 画节点和连线）
4. 事件监听 mousedown/mousemove/mouseup（→ 学拖拽交互）
5. saveDag() / loadDagToCanvas()（→ 学 DAG JSON 结构）
6. renderControls()（→ 学控件从后端加载到左侧面板）
```

> 💡 **自测题**：看完后尝试回答——画布上"从节点右侧拖出一条线到另一个节点"这个过程，代码是怎么实现的？

------

### 📙 阶段三：后端入门 — 从 REST API 开始（5-7天）

后端是 **Spring Boot 3 + JPA + H2 数据库**，包结构清晰：

```
backend/src/main/java/com/datastream/mvp/
├── MvpBackendApplication.java    ← 入口
├── config/                        ← 配置
├── controller/                     ← REST API（前端调的就是这里）
├── model/                          ← 数据模型（对应数据库表）
├── repository/                     ← 数据库操作
├── service/                        ← 业务逻辑
├── dag/                            ← DAG 执行引擎（核心难点）
└── plugin/                         ← 插件热加载
```

**前置知识（最少够用原则）：**

| 要学的           | 学多少                                                |
| ---------------- | ----------------------------------------------------- |
| Java 基础        | 类、接口、注解、依赖注入（@Autowired）                |
| Spring Boot 基础 | @RestController、`@GetMapping/`@PostMapping、@Service |
| JPA 基础         | @Entity、JpaRepository 的 CRUD 方法                   |

**推荐的免费教程：**

- [Spring Boot 官方 Quickstart](https://spring.io/quickstart)（英文，但代码很简洁）
- 或者 B 站搜"Spring Boot 入门"看前 5 集就够了

**对照项目读代码的顺序：**

```
1. MvpBackendApplication.java         ← 入口，只有一行
2. model/JobDefinition.java           ← 最简单的数据类
3. controller/JobController.java      ← API 接口，最直观
4. service/JobService.java            ← 业务逻辑
5. repository/JobDefinitionRepository ← 数据库查询，极其简单
6. dag/DagDefinition.java             ← DAG JSON 的 Java 映射
7. dag/DagExecutor.java               ← 接口（看明白接口定义就行）
8. dag/FlinkDagExecutor.java          ← Flink 执行（可以先跳过细节）
```

> 💡 **自测题**：POST /api/jobs 这个接口，从 HTTP 请求到存入数据库，经过了哪些类？调用链画出来。

------

### 📕 阶段四：核心进阶 — DAG + Flink 引擎（7-10天）

这是项目最难的部分，**可以先只理解概念，不深入代码细节**。

**要理解的核心概念：**

```
DAG JSON 结构：
{
  "nodes": [
    {"id":"n1", "type":"csv_input",  "params":{"path":"data.csv"}},
    {"id":"n2", "type":"field_concat", "params":{"fields":["a","b"]}},
    {"id":"n3", "type":"csv_output",  "params":{"path":"out.csv"}}
  ],
  "edges": [
    {"source":"n1","target":"n2"},
    {"source":"n2","target":"n3"}
  ]
}
```

**学习顺序：**

1. 看懂 `DagDefinition.java` — DAG JSON 怎么映射成 Java 对象
2. 看懂 `DagTranslationService.java` — DAG JSON 怎么翻译成 Flink 代码
3. 看懂 `FlinkDagExecutor.java` — 怎么把翻译好的代码提交给 Flink 集群
4. 看懂 `LocalDagExecutor.java` — 不用 Flink 也能本地模拟执行的兜底方案

**Flink 只学这些够用：**

- DataStream 的三要素：Source → Transformation → Sink
- Table API 比 DataStream 简单，本项目用的是 Table API
- 不需要深入 State/Checkpoint/Watermark 这些高级概念

------

### 📗 阶段五：AI Agent 拓展（5-7天）

AI 侧车是一个独立的 **Python FastAPI 服务**（`ai_sidecar/main.py`），和后端松耦合。

代码只有 **70 行**，核心结构：

```
POST /nlp-to-dag     ← 用户说"我想从 CSV 读数据，拼字段，写到 MySQL"，自动生成 DAG
POST /opsbot/analyze ← 运维机器人，分析作业失败原因
POST /nl2data/query  ← 自然语言转 SQL
```

**学习路线：**

1. 学 Python 基础（函数、async/await、类型注解）— 2天
2. 学 FastAPI 基础（一个文件就够了）— 1天
3. 看懂 `main.py` 全部代码 — 半天
4. 装 Ollama 跑通本地模型（可选）— 1天

------

### 🗺️ 总路线图

```
第1周 ─ 阶段一 (跑起来) + 阶段二前半 (HTML/CSS/JS基础)
第2周 ─ 阶段二后半 (app.js 通读)
第3周 ─ 阶段三前半 (Java + Spring Boot 基础)
第4周 ─ 阶段三后半 (后端代码通读)
第5-6周 ─ 阶段四 (DAG + Flink 核心)
第7周 ─ 阶段五 (AI Sidecar)
```

------

### 🛠️ 一些自学小技巧

1. **不要从头到尾读代码** — 按"数据流"读：用户点了保存 → 前端拼 JSON → fetch POST → 后端接收 → 存数据库。追着一条请求走。
2. **在代码里加 console.log / System.out.println** — 打印变量看看实际值是什么，最有效的理解方式。
3. **改一行代码看看效果** — 比如改改 `app.js` 里 `NodalWidth` 从 140 改成 200，看画布节点变宽。改坏了 git 还原就行。
4. **先从"山寨"开始** — 不要自己从头写，而是在现有代码上做小修改（加一个新控件类型、改改颜色主题），成就感来得快。
5. **遇到不懂的** — 这个项目每个模块都不大，直接问我具体哪个文件哪行代码看不懂，我可以逐行解释。