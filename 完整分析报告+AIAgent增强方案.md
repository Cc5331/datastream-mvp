# 🚀 数据流任务管理系统 — 完整可行性分析 & AI Agent 增强方案

> 生成日期：2026-07-05 | 基于题目文档 & 架构图分析  
> 硬件约束：8vCPU / 32G RAM / 300G HDD | 离线环境（无外网）

---

## 一、项目可行性分析 ✅

### 1.1 整体判断：完全可行，且具备中高竞争力

| 维度 | 评估 | 说明 |
|------|------|------|
| 技术难度 | ⭐⭐⭐⭐☆ | 核心难点在 Flink DAG 动态编译、插件化架构；其余为常规 CRUD |
| 团队匹配度 | ✅ | Java + Vue/React + Python 是主流组合，人力充足 |
| 硬件约束 | ✅ 达标 | 8vCPU/32G 跑 Flink Standalone 集群足够，300G HDD 注意 I/O 瓶颈 |
| 时间匹配 | ✅ 充裕 | MVP 1个月 → AI 增强 2个月 → 深度优化 2-3个月，周期合理 |
| 创新空间 | ⭐⭐⭐⭐⭐ | AI Agent 嵌入几乎无竞品做过，是天然差异化亮点 |

### 1.2 技术风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| Flink 动态 DAG 编译 | 高 | 用 Flink Table API + SQL 动态拼接，不走 DataStream 底层 API |
| 离线环境依赖 | 中 | 用 Maven/Gradle 离线仓库 + Nexus 私服；前端 npm 包提前下载 |
| HDD 机械盘 I/O | 中 | CSV/Excel 测试用顺序读写；生产级场景可用内存映射 |
| AI 模型本地部署 | 中 | Qwen2.5-7B 量化后 4-6G 显存，用 API 加速库裁剪占用 |

---

## 二、核心架构优化建议

### 2.1 微服务拆分（适合团队分工）

```
frontend (Vue/React 画布+参数面板)
    -> job-api (作业管理、DAG解析提交)
        -> control-api (控件注册表、插件热加载)
            -> monitor (状态监控、Flink轮询)
                -> Flink Cluster (Standalone)
```

好处：前端/后端/引擎分离，3-4人可并行开发，互不阻塞。

### 2.2 控件插件化热加载（加分关键点：新增控件不重启服务）

```java
// SPI + 类隔离加载器
public interface ControlPlugin {
    String getType();
    DataControl createInstance();
}
```

将每个控件打包为独立 JAR，放在 plugins/ 目录下，用 URLClassLoader 动态加载。演示时放入新 JAR -> 前端立刻出现新控件。

### 2.3 DAG 引擎抽象层

```
DAG JSON -> DAGExecutor 接口 -> FlinkDagExecutor (生产用)
                              -> LocalDagExecutor (开发调试用)
```

- 开发调试不用起 Flink 集群
- 未来可接 Spark Structured Streaming
- 单元测试可直接 mock

### 2.4 事件驱动解耦

作业生命周期状态变更 -> 通过事件总线广播：JobSubmitted -> JobRunning -> JobFailed -> JobCompleted

前后端解耦，方便未来加 AI Agent 监听事件。

---

## 三、性能优化建议

### 3.1 关键瓶颈分析

| 组件 | 瓶颈 | 优化方向 |
|------|------|----------|
| HDD I/O | 300G 机械盘读写 | CSV 测试用大文件顺序读，避免随机 I/O |
| Flink Checkpoint | 状态后端写 HDD | 用 FsStateBackend 配本地路径，增量 Checkpoint |
| Redis 查询 | 网络延迟 | 用 AsyncDataStream，连接池化 |
| 前端画布 | 大量节点时卡顿 | 使用 AntV X6 的虚拟滚动 + 懒渲染 |

### 3.2 性能测试方案（比赛验收硬指标）

报告需要包含：
1. 不同数据量：10万 / 100万 / 1000万 行 CSV->拼接->写 CSV
2. 不同并行度：1 / 2 / 4 / 8 并行度下的吞吐量
3. 资源利用率：CPU / 内存 / 磁盘 I/O 监控
4. 图表：吞吐量-并行度曲线（展示近线性扩展）

示例跑分格式：
```
并行度=1:  12,000 rows/s
并行度=2:  23,500 rows/s  (95% 线性度)
并行度=4:  46,000 rows/s  (92% 线性度)
并行度=8:  85,000 rows/s  (85% 线性度)
```

---

## 四、开发效率优化建议

1. **DAG JSON -> Flink SQL 直接翻译**：保存 DAG 后自动生成等价 Flink SQL，调试时可直接在 Flink SQL CLI 运行
2. **控件代码生成器**：根据数据库 control_registry 表自动生成控件接口脚手架代码
3. **本地开发环境脚本**：Docker Compose 一键启动 MySQL + Redis + Kafka + Flink

---

## 五、AI Agent 增强方案（核心亮点）

> 这是本项目最大差异化亮点。在大部分参赛队还在做 Flink + 画布 传统方案时，嵌入 AI Agent 可以形成降维打击。

### 5.1 整体架构：AI Agent Sidecar

```
                   数据流任务管理系统
  +----------+  +----------+  +----------------------+
  | 前端画布   |  | 后端服务  |  |  AI Agent Sidecar   |
  | (Vue)    |->| (Java)   |->|  (Python FastAPI)    |
  +----------+  +----+-----+  +----------+-----------+
                      |                  |
                 +----v-----+     +------v-------+
                 | Flink    |     | LLM 服务      |
                 | Cluster  |     | (本地/云端)    |
                 +----------+     +--------------+
```

为什么用 Python 做 Sidecar？
- Python 是 AI/LLM 生态最强的语言（LangChain, Transformers, LlamaIndex）
- 与 Java 后端通过 REST/gRPC/MQ 通信，松耦合
- 即使离线环境，Python 部署也比 Java 的 AI 库更方便

---

### 5.2 Agent 1：NLP -> DAG 智能构建器（最高优先级）

**用户输入一句话生成作业**：

```
用户输入: 每隔10秒读MySQL的order表，过滤金额>1000的订单，
          左关联Redis的用户画像数据，结果写到Kafka的order_topic
    | LLM 解析
输出 DAG JSON 并在前端渲染 -> 用户确认/编辑 -> 一键上线
```

**技术实现**：
- Prompt 设计：将控件清单 + 参数 Schema + 示例 DAG 作为上下文传给 LLM
- 纠错机制：LLM 输出的 DAG JSON 经过 Schema 校验 + 拓扑排序验证
- 交互方式：在画布上方加一个输入框，用户输入需求，点击智能生成按钮

**演示亮点**：视频中可以展示输入一句话 -> 自动生成完整作业 -> 一键上线运行，极具视觉冲击力。

---

### 5.3 Agent 2：智能运维 Agent - OpsBot

| 功能 | 说明 |
|------|------|
| 异常检测 | 监控作业延迟/吞吐量突降 |
| 根因分析 | 异常发生时用 LLM 分析日志 + 指标快照 |
| 自动修复 | 重启失败作业/扩容 |
| 健康报告 | 每日/周作业运行概览 |
| 问答对话 | 为什么作业 A 今天慢了？ |

**对话示例**：
```
用户: 为什么 csv_to_mysql 作业今天慢了3倍？
OpsBot: 检测到以下异常：
1. 源数据量从昨日的 50万行增加到 210万行（+320%）
2. 并行度未调整（仍为2）
3. 磁盘I/O等待时间上升 150%
建议：将并行度调整为4，或拆分作业。需要我自动调整吗？
```

---

### 5.4 Agent 3：NL2Data 智能查询 Agent

**场景**：用户想查数据但不会写 SQL

```
用户输入: 查一下昨天order表里金额最大的10笔订单
    |
Agent 自动生成 Flink SQL -> 提交到 Flink Session 执行 -> 返回结果表格 + 自然语言总结
```

---

### 5.5 Agent 4：数据质量 & 血缘智能分析

1. **数据血缘可视化**：自动解析 DAG，生成字段级血缘图
2. **质量规则推荐**：根据字段类型自动推荐质检规则
3. **异常数据自动分类**：脏数据到达时自动分类并给出处理建议

---

### 5.6 Agent 5：AI 辅助代码生成 - 控件开发助手

开发者在后台点击新建控件 -> 填写基本信息 -> Agent 生成控件实现代码 + 参数 Schema -> 下载打包热加载

---

## 六、LLM 选型与部署策略

### 6.1 混合架构

```
有网环境 -> DeepSeek / Qwen API（低延迟、高质量）
离线环境 -> 本地 Qwen2.5-7B / ChatGLM3-6B 量化版
```

### 6.2 本地部署方案

| 模型 | 显存需求 | 适合场景 |
|------|---------|---------|
| Qwen2.5-7B (Int4) | 4-6G | DAG 生成、NL2SQL |
| ChatGLM3-6B (Int4) | 4-6G | 对话、代码生成 |
| Qwen2.5-1.5B (Int4) | 1-2G | 简单分类、规则匹配 |

部署方式：Ollama serve qwen2.5:7b 一键启动

### 6.3 离线环境应对

- 比赛验收时若没网：提前下载 Ollama + 模型文件到 U 盘
- 演示时本地起 Ollama 服务
- 若机器无 GPU：用 Qwen2.5-1.5B（纯 CPU 也能跑），或降级为规则引擎

---

## 七、实施路线图（3个阶段）

### Phase 1：MVP 基础版（第1个月）

| 模块 | 工作内容 |
|------|---------|
| 后端基础 | Spring Boot 项目搭建、数据库设计、控件注册表 CRUD |
| Flink 集成 | Flink Standalone 集群部署、DAG JSON -> Flink 作业翻译 |
| 控件开发 | 基础控件：CSV/Excel/MySQL/Kafka 输入输出 + 字段拼接 + XML<->JSON |
| 前端 | AntV X6 画布拖拉拽、参数面板动态渲染、作业管理页面 |
| 性能测试 | 8vCPU 环境跑基准测试，生成性能报告 |

### Phase 2：AI 增强版（第2-3个月）

| 模块 | 工作内容 |
|------|---------|
| AI Sidecar | Python FastAPI 服务搭建，LLM 集成（Ollama/API） |
| NLP->DAG | Prompt 工程 + DAG JSON 输出解析 + Schema 校验 |
| OpsBot | 作业监控 + 异常检测 + 简单自动修复 |
| NL2Data | Text-to-SQL 查询接口 |
| 挑战控件 | Redis 异步查询、HDFS/PostgreSQL/Oracle Connector |

### Phase 3：深度智能版（第3-4个月）

| 模块 | 工作内容 |
|------|---------|
| 数据血缘 | 字段级血缘追踪 + 可视化展示 |
| 质量规则 | 智能推荐 + 异常数据自动分类 |
| 代码生成 | 控件开发助手 |
| 演示打磨 | 5分钟视频脚本、高分文档完善 |

---

## 八、比赛致胜策略

### 8.1 差异化亮点矩阵

| 亮点 | 独创性 | 演示效果 | 推荐 |
|------|--------|---------|------|
| Flink 动态 DAG | 普通队做不到 | 4星 | 必须做 |
| 控件热加载 | 少部分队能做 | 5星 | 必须做 |
| AsyncDataStream Redis | 大部分队想不到 | 4星 | 必须做 |
| NLP->DAG 智能构建 | 几乎无队能做 | 5星 | 必杀技 |
| OpsBot 对话 | 几乎无队能做 | 5星 | 必杀技 |
| 数据血缘追踪 | 少部分队能做 | 4星 | 建议做 |

### 8.2 演示视频脚本（5分钟）

```
0:00-0:30  项目概述 + 架构展示
0:30-1:30  一句话生成作业：输入需求 -> 自动生成 DAG -> 一键上线
1:30-2:00  手动拖拉拽微调 + 参数配置（展示控件可扩展性）
2:00-2:30  作业运行监控 + OpsBot 对话
2:30-3:00  性能展示：扩容前 vs 扩容后吞吐量对比
3:00-3:30  NL2Data 查询
3:30-4:00  挑战目标展示：Redis 异步查询
4:00-4:30  数据血缘追踪
4:30-5:00  总结 + 技术亮点回顾
```

### 8.3 关键提醒

1. 文档早于代码：5份交付物在 MVP 阶段就搭好框架
2. 控件热加载是得分王牌：展示放入 JAR -> 前端自动出现新控件
3. AI Agent 做成加分项、不要做成依赖项：AI 功能失败时基础功能仍能完整演示
4. 性能测试用图表说话：吞吐量-并行度曲线 / 不同数据量耗时对比
